'use strict'
module.exports = function(app){
  //drop and create table so data is lost
  //app.dataSources.mysql.automigrate()
  //app.dataSources.mongodb.autoupdate
/*
  app.dataSources.mongodb.autoupdate(['bank'],err =>{
    if(err) throw err;
    console.log('Model sync');
  });
*/
}
